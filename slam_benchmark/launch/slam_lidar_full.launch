<launch>
  <!-- Full Stage 9 LiDAR SLAM baseline. Stereo stays disabled unless requested. -->
  <arg name="use_sim_time" default="true"/>
  <arg name="use_rviz" default="true"/>
  <arg name="gazebo_gui" default="true"/>
  <arg name="scan_topic" default="/lidar/scan"/>
  <arg name="base_frame" default="base_footprint"/>
  <arg name="odom_frame" default="odom"/>
  <arg name="map_frame" default="map"/>
  <arg name="model_name" default="omni_robot"/>
  <arg name="use_gmapping" default="true"/>
  <arg name="use_gazebo_model_controller" default="true"/>
  <arg name="use_gazebo_truth_odom" default="true"/>
  <arg name="use_stereo" default="false"/>

  <param name="/use_sim_time" value="$(arg use_sim_time)"/>

  <include file="$(find robot_description)/launch/gazebo_lidar_sim.launch">
    <arg name="use_sim_time" value="$(arg use_sim_time)"/>
    <arg name="gui" value="$(arg gazebo_gui)"/>
  </include>

  <include if="$(arg use_gazebo_model_controller)" file="$(find omni_base_controller)/launch/gazebo_model_controller.launch">
    <arg name="use_sim_time" value="$(arg use_sim_time)"/>
    <arg name="model_name" value="$(arg model_name)"/>
    <arg name="cmd_vel_topic" value="/cmd_vel"/>
  </include>

  <include if="$(arg use_gazebo_truth_odom)" file="$(find omni_base_controller)/launch/gazebo_truth_odom.launch">
    <arg name="use_sim_time" value="$(arg use_sim_time)"/>
    <arg name="model_name" value="$(arg model_name)"/>
    <arg name="odom_topic" value="/odom"/>
    <arg name="odom_frame" value="$(arg odom_frame)"/>
    <arg name="base_frame" value="$(arg base_frame)"/>
    <arg name="publish_tf" value="true"/>
  </include>

  <include if="$(arg use_stereo)" file="$(find stereo_pipeline)/launch/stereo_slam_pipeline.launch">
    <arg name="scan_topic" value="/scan"/>
    <arg name="target_frame" value="base_link"/>
  </include>

  <include if="$(arg use_gmapping)" file="$(find slam_benchmark)/launch/gmapping_lidar.launch">
    <arg name="use_sim_time" value="$(arg use_sim_time)"/>
    <arg name="scan_topic" value="$(arg scan_topic)"/>
    <arg name="base_frame" value="$(arg base_frame)"/>
    <arg name="odom_frame" value="$(arg odom_frame)"/>
    <arg name="map_frame" value="$(arg map_frame)"/>
  </include>

  <node if="$(arg use_rviz)" pkg="rviz" type="rviz" name="rviz_lidar_slam"
        args="-d $(find slam_benchmark)/rviz/lidar_slam.rviz" required="false"/>
</launch>
