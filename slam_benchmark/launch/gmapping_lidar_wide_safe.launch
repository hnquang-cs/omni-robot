<launch>
  <!-- Stage 9 wide and conservative Gmapping profile for safe LiDAR mapping. -->
  <arg name="scan_topic" default="/lidar/scan"/>
  <arg name="base_frame" default="base_footprint"/>
  <arg name="odom_frame" default="odom"/>
  <arg name="map_frame" default="map"/>
  <arg name="use_sim_time" default="true"/>

  <param name="/use_sim_time" value="$(arg use_sim_time)"/>

  <node pkg="gmapping" type="slam_gmapping" name="slam_gmapping_lidar_wide_safe" output="screen">
    <remap from="scan" to="$(arg scan_topic)"/>
    <rosparam command="load" file="$(find slam_benchmark)/config/gmapping_lidar_wide_safe.yaml"/>
    <param name="base_frame" value="$(arg base_frame)"/>
    <param name="odom_frame" value="$(arg odom_frame)"/>
    <param name="map_frame" value="$(arg map_frame)"/>
  </node>
</launch>
